name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
  pull_request:
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: '1'
  DOTNET_NOLOGO: '1'
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages

jobs:
  build-test:
    name: Windows / .NET 10 / Release
    runs-on: windows-2025
    timeout-minutes: 20

    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          global-json-file: global.json
          architecture: x64
          cache: true
          cache-dependency-path: '**/packages.lock.json'

      - name: Show environment
        run: dotnet --info

      - name: Restore
        run: dotnet restore QuickPods.sln --locked-mode

      - name: Restore installer toolchain
        run: dotnet restore installer/QuickPods.Setup/QuickPods.Setup.wixproj --locked-mode

      - name: Audit NuGet vulnerabilities
        run: ./build/Test-DependencyVulnerabilities.ps1

      - name: Verify formatting
        run: dotnet format QuickPods.sln --verify-no-changes --no-restore --severity warn --verbosity minimal

      - name: Build
        run: dotnet build QuickPods.sln -c Release --no-restore -p:ContinuousIntegrationBuild=true

      - name: Test
        id: test
        run: >-
          dotnet test QuickPods.sln
          -c Release
          --no-build
          --no-restore
          --logger "trx;LogFilePrefix=quickpods"
          --results-directory TestResults
          --collect:"XPlat Code Coverage"
          --
          RunConfiguration.TreatNoTestsAsError=true

      - name: Upload test results
        if: ${{ always() && steps.test.outcome != 'skipped' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-results
          path: TestResults/**
          if-no-files-found: error
          retention-days: 3

      - name: Publish release candidate
        if: ${{ github.event_name != 'pull_request' }}
        run: >-
          ./build/Publish-ReleaseCandidate.ps1
          -OutputDirectory artifacts/rc
          -Version 0.1.0-ci.${{ github.run_number }}

      - name: Publish per-user MSI
        if: ${{ github.event_name != 'pull_request' }}
        run: >-
          ./build/Publish-Installer.ps1
          -OutputDirectory artifacts/installer
          -Version 0.1.0-ci.${{ github.run_number }}
          -PayloadDirectory artifacts/rc/QuickPods
          -SkipPayloadPublish
          -SkipRestore

      - name: Upload release candidate
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: quickpods-release-candidate
          path: |
            artifacts/rc/QuickPods-*-win-x64.zip
            artifacts/rc/SHA256SUMS.txt
            artifacts/rc/QuickPods/artifact-manifest.json
          if-no-files-found: error
          retention-days: 3

      - name: Upload per-user MSI
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: quickpods-per-user-msi
          path: |
            artifacts/installer/QuickPods-*-win-x64.msi
            artifacts/installer/SHA256SUMS.txt
            artifacts/installer/installer-manifest.json
            artifacts/installer/payload-artifact-manifest.json
          if-no-files-found: error
          retention-days: 3
